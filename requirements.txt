flask>=2.3.0
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
Werkzeug>=2.3.0
Pillow>=10.0.0

# ── ECG image / PDF digitisation (ECGtizer) ──────────────────────────────────
# Needed only if you want to analyse ECG *images* or *PDFs*. Raw .pt / .mat / .dat
# work without these. ECGtizer itself is a repo, not on PyPI — drop it into
# ./ecgtizer (see testing-ecgtizer-v2.ipynb) and install its dependencies:
opencv-python>=4.8.0
PyMuPDF>=1.23.0
wfdb>=4.1.0
